// JS file so it is compatible with tailwind.config.js

const SWIPER_SHOWCASE_HEIGHT = 388
const SWIPER_SHOWCASE_WIDTH = Math.floor(SWIPER_SHOWCASE_HEIGHT * (16 / 9))

module.exports = {
  SWIPER_SHOWCASE_HEIGHT,
  SWIPER_SHOWCASE_WIDTH,
}
